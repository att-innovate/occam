# /etc/hiera.yaml
# This file created by Ansible.
# Disable gpg and puppetdb for first run
---
:hierarchy:
  - "secrets/zone1"
  - "fqdns/%{::fqdn}"
  - "zones/zone1"
  - "hostgroups/%{::hostgroup}"
  - "common"

# :gpg:
#   :key_dir: /etc/puppet/keyrings
#   :datadir: "/etc/puppet/environments/%{::environment}/hiera"

:yaml:
  :datadir: "/etc/puppet/environments/%{::environment}/hiera"


:backends:
  - yaml
  # - gpg
  # - puppetdb